package com.rork.varabondhu.ui.theme

import androidx.compose.ui.graphics.Color

/** Deep forest green taken from the rickshaw pin logo. */
val BrandGreenDeep: Color = Color(0xFF017335)

/** Filled-button green sampled from the reference screens. */
val ButtonGreen: Color = Color(0xFF017937)

/** Brighter green used for the "Bondhu" half of the wordmark. */
val BrandGreen: Color = Color(0xFF0F7A33)

/** Light green highlight found inside the logo's rickshaw hood. */
val BrandGreenSoft: Color = Color(0xFF249551)

/** Pale mint canvas that matches the illustration's own background exactly. */
val MintCanvas: Color = Color(0xFFF1F8F3)

/** Near-white tint used at the very top of the canvas wash. */
val MintCanvasTop: Color = Color(0xFFFAFDFB)

/**
 * Exact flat colour of the sky area inside `city_street.webp`, so the canvas can be
 * extended above the artwork without a visible seam.
 */
val IllustrationSky: Color = Color(0xFFF1F9F4)

/** Soft mint glow used behind illustrations. */
val MintGlow: Color = Color(0xFFE8F4EE)

/** Road grey sampled from the bottom edge of the city illustration. */
val RoadGrey: Color = Color(0xFFDDE9E7)

val Ink: Color = Color(0xFF111814)
val InkMuted: Color = Color(0xFF6B7A70)

/** Sheet / page surfaces. */
val CardWhite: Color = Color(0xFFFFFFFF)
val PageWhite: Color = Color(0xFFF0F3F1)

/** Input field chrome. */
val FieldBorder: Color = Color(0xFFE8ECE9)
val FieldIcon: Color = Color(0xFF8C9A91)
val FieldPlaceholder: Color = Color(0xFFA9B4AC)

/** Inline validation colour. */
val DangerRed: Color = Color(0xFFC0392B)
